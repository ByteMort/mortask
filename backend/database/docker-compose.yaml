volumes:
  data:


services:
  db:
    image:  postgres:18
    environment:
      POSTGRES_USER:  "rusty"
      POSTGRES_PASSWORD:  "rusty"
      POSTGRES_DB:  "rust_db"
    volumes:
      -  data:/var/lib/postgresql
    ports:
      -  "5555:5432"
    