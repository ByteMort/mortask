[package]
name = "mortask_backend"
version = "0.1.0"
edition = "2024"
authors = ["ByteMort <bytemort@gmail.com>"]
description = "MorTask Backend"
repository = "https://github.com/ByteMort/mortask"
license = "LicenseRef-Proprietary"

[dependencies]
tokio = { version = "1.52.3", features = ["full"] }
axum = "0.8.9"
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.150"

tracing = "0.1.44"
tracing-subscriber = { version = "0.3.23", features = ["env-filter"] }

sqlx = { version = "0.9.0", features = ["macros", "postgres", "runtime-tokio", "chrono"]}
chrono = { version = "0.4.45", features = ["serde"] }

validator = { version = "0.20.0", features = ["derive"]}
axum-valid = "0.25.0"

bcrypt = "0.19.2"

jsonwebtoken = { version = "10.4.0", features = ["rust_crypto"]}
dotenvy = "0.15.7"

rand = "0.10.2"
lettre = { version = "0.11.22", default-features=false, features = ["tokio1-rustls-tls", "smtp-transport", "builder"]}
